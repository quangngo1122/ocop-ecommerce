type User {
  _id: ID!
  email: String!
  role: UserRole!
  profile: UserProfile
  createdAt: DateTime!
  updatedAt: DateTime!
}

type UserProfile {
  firstName: String
  lastName: String
  avatar: String
  phone: String
}

enum UserRole {
  admin
  user
  seller
}

scalar DateTime
